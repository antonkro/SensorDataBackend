package sensordata.app.utils;
/**
 * is used to differ push data and stream data
 * For now its only possibly to store pushdata
 * 
 * @author Anton Kroisant
 *
 */
public enum ParamType {
	STREAMDATA,
	PUSHDATA
}
